# TPW 发布更新流程（v6.6）

> 从修改文件到用户收到更新，一键完成。

---

## 快速上手

```powershell
# ① 修改资源/代码

# ② 一键打包 + 自动发布（Nuitka 编译 → ZIP → Git 提交推送 → 创建标签）
.\build.ps1 -Version "6.6.0.6" -CreateZip -AutoTag

# ③ 完成。等 1-2 分钟后用户点击「增量更新」即可
```

---

## build.ps1 参数速查

| 参数 | 作用 | 适用场景 |
|------|------|---------|
| `-Version "X.X.X.X"` | 指定版本号 | **每次发布必用** |
| `-CreateZip` | 生成 ZIP 分发包 | 首次用户需要完整安装包 |
| `-AutoTag` | 自动 `git commit → push master → tag → push tag` | **推荐始终开启**，一键完成发布 |
| `-QuickUpdate` | 跳过 Nuitka 编译（`-SkipNuitka -CreateZip` 的快捷方式） | **仅限同版本号改资源**，新版本号首次必须完整编译 |
| `-DryRun` | 试运行，仅生成清单不打包 | 预览清单内容 |
| `-SkipNuitka` | 跳过 Nuitka 编译 | 已编译过，仅更新资源 |
| `-SkipManifest` | 跳过清单生成 | 调试用 |
| `-Changelog "内容"` | 附带更新日志 | 用户端显示更新内容 |
| `-TargetDir "路径"` | 自定义输出目录 | 默认 `D:\test_py\tpw\打包\tpw{版本号}` |

---

## 三种发布模式

### 模式 1：一键完整发布（新版本 / exe 有变更）

```powershell
.\build.ps1 -Version "6.6.0.6" -CreateZip -AutoTag
```

自动执行 8 步：
```
Step 0  更新版本号 + 生成 manifest.json
Step 1  清理旧构建产物
Step 2  Nuitka 编译 → 躺平王服务端.exe（3-8 分钟）
Step 3  准备目标目录
Step 4  复制 Nuitka 产物 (exe/dll/pyd)
Step 5  复制资源文件 (手牌/场景/配置/OCR/增量更新系统)
Step 6  完整性验证
Step 7  创建 ZIP 分发包
Step 8  ★ git commit → push master → git tag v6.6.0.6 → push tag
```

### 模式 2：快捷资源更新（仅改资源文件，exe 不变）

```powershell
.\build.ps1 -Version "6.6.0.6" -QuickUpdate -AutoTag
```

> ⚠️ **前提**：目标目录已有完整 exe。*不能用于新版本号首次发布！*

### 模式 3：试运行预览

```powershell
.\build.ps1 -Version "6.6.0.6" -DryRun
```

仅生成 manifest.json 和 update_config.json，不打包不发布。

---

## 完整发布流程（首次 / 重大更新）

```
┌─────────────────────────────────────────────────────┐
│ 1. 修改资源或代码                                     │
│    ├─ 资源: 手牌/ 场景/ 配置/ models/                │
│    ├─ 脚本: tpw_server.py / DmTool.py 等             │
│    └─ 新增文件类型? → 编辑 update_config.json         │
├─────────────────────────────────────────────────────┤
│ 2. 一键发布                                          │
│    .\build.ps1 -Version "6.6.0.6" -CreateZip -AutoTag│
├─────────────────────────────────────────────────────┤
│ 3. 等待 CDN 同步 (~1-2 分钟)                         │
│    jsDelivr 自动拉取 GitHub 最新 tag                  │
├─────────────────────────────────────────────────────┤
│ 4. 用户端点击「增量更新」                              │
│    → 下载 manifest.json → MD5 比对 → 仅下载变更文件    │
└─────────────────────────────────────────────────────┘
```

---

## Git Tag 工作机制（★ 关键）

`-AutoTag` 自动执行以下 Git 操作：

```powershell
git add manifest.json update_config.json models/tpwVer.txt
git commit -m "v6.6.0.6: 发布更新"
git push origin master
git tag v6.6.0.6           # ← 创建版本标签
git push origin v6.6.0.6   # ← 推送到 GitHub
```

**为什么 Tag 是必须的：**

```
server_url = "https://cdn.jsdelivr.net/gh/lvpingJava/tpw@v6.6.0.6"
                                                           ↑
                                          jsDelivr 依赖 Git tag 定位文件
                                          无 tag → CDN 404 → 回退 @master → 旧版本
```

CDN 三级回退链：
1. `jsDelivr @v{版本号}` ← 主通道，无缓存
2. `raw.githubusercontent.com @v{版本号}` ← 备选
3. `jsDelivr @master` ← 最后兜底（可能有 7 天缓存）

---

## 更新内容范围

| 更新内容 | 增量更新可达？ | 说明 |
|---------|:---:|------|
| 手牌/场景/配置 图片和文件 | ✅ | 资源文件在 CDN 清单中 |
| models/ 版本文件/图标 | ✅ | 同上 |
| wxauto/ 自动化脚本 | ✅ | 同上 |
| OCR 引擎和模型 | ✅ | matplotlibes/ 目录 |
| 根目录 Python 脚本 | ✅ | .py 文件在清单中，但不影响已编译 exe |
| **躺平王服务端.exe** | ❌ | 超过 50MB，不在 CDN 清单中，需重新下载完整 ZIP |

> **注意**：Python 脚本变更后增量更新会推送 .py 文件，但正在运行的 exe 不会自动重编译。如需 exe 变更生效，用户必须下载完整 ZIP 安装包。

---

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| 更新显示旧版本 | Git tag 未推送 | 手动执行 `git tag vX.X.X.X; git push origin vX.X.X.X` |
| `-QuickUpdate` 后缺 exe | 跳过了 Nuitka 编译，这是**新版本号**的首次发布 | 改用 `-CreateZip -AutoTag`（去掉 `-QuickUpdate`） |
| `NativeCommandError` | git 输出到 stderr 被 PowerShell 误判 | v6.6.0.5+ 已修复 |
| raw.githubusercontent.com 超时 | GitHub Raw 在国内不稳定 | 确保 Git tag 已推送，让 jsDelivr 主通道生效 |
| 中文文件名下载失败 | URL 未编码 | v6.6.0.5+ 已自动 URL 编码 |
| 更新后程序崩溃 | Nuitka 系统目录被误删 | v6.6.0.1+ 已修复保护机制 |

---

## 版本号规范

格式：`主版本.次版本.修订号.构建号`

```
6.6.0.5 → 6.6.0.6  资源小更新
6.6.0.5 → 6.6.1.0  功能更新
6.6.0.5 → 6.7.0.0  大版本升级
```

每次发布必须使用新版本号，向前递增。

---

> 📅 最后更新: 2026-07-14 ｜ 📦 适用版本: v6.6.0.5+

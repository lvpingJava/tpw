# TPW 发布更新流程（精简版）

> 从修改文件到用户收到更新，一条龙操作指南。

---

## 快速上手（日常更新）

```powershell
# ① 修改资源文件（手牌/场景/配置等）

# ② 打开 IDEA 终端（已自动激活 .venv），一键打包
.\build.ps1 -Version "6.5.0.0"

# ③ 推送清单到 GitHub（打包脚本末尾会提示，直接复制执行）
git add manifest.json update_config.json models/tpwVer.txt
git commit -m "v6.5.0.0: 更新手牌数据"
git push origin master

# ④ 完成。用户点「增量更新」即可获取变更
```

---

## 详细步骤

### 第一步：修改文件

| 更新内容 | 修改位置 | 用户能增量更新？ |
|---------|---------|:---:|
| 手牌识别图片 | `手牌/` 目录 | ✅ 是 |
| 场景图片 | `场景/` 目录 | ✅ 是 |
| 配置文件/DLL | `配置/` 目录 | ✅ 是 |
| Python 脚本 | `tpw_server.py` 等 | ❌ 否（编译进 exe，需重装） |

### 第二步：打包

```powershell
# 激活 venv（IDEA 终端已自动激活则跳过）
.\.venv\Scripts\Activate.ps1

# 指定版本号打包
.\build.ps1 -Version "6.5.0.0"

# 脚本自动完成：更新版本号 → 生成 manifest.json → Nuitka 编译 → 复制资源 → 验证
```

> 打包产物在 `D:\test_py\tpw\打包\tpw6.5.0.0\`，用于分发给新用户。

### 第三步：发布到 GitHub

```powershell
git add manifest.json update_config.json models/tpwVer.txt
git commit -m "v6.5.0.0: 更新说明"
git push origin master
```

> ⚠️ **只提交这 3 个文件**，不要 `git add -A`（会把 exe/dll 也提交上去）。

### 第四步：用户更新

用户打开程序 → 点击「增量更新」按钮 → 系统自动：
1. 从 CDN 下载最新 `manifest.json`
2. 逐文件 MD5 比对（只下载变动的文件）
3. 清理废弃文件（保护 `.idea`、`.git`、`.venv` 不会被删）
4. 提示重启生效

---

## 常用命令速查

| 操作 | 命令 |
|------|------|
| 打包（指定版本） | `.\build.ps1 -Version "6.5.0.0"` |
| 打包（读配置文件） | `.\build.ps1` |
| 仅生成清单不打包 | `py .\incremental_update\builder.py --version "6.5.0.0"` |
| 推送清单 | `git add manifest.json update_config.json models/tpwVer.txt` + `git commit` + `git push` |
| 刷新 CDN 缓存 | 浏览器打开 `https://purge.jsdelivr.net/gh/lvpingJava/tpw@master/manifest.json` |
| 验证 CDN 可访问 | 浏览器打开 `https://cdn.jsdelivr.net/gh/lvpingJava/tpw@master/manifest.json` |

---

## 注意事项

1. **版本号递增**：每次发布必须用新版本号（如 `6.4.9.7` → `6.4.9.8`）
2. **只提交清单**：Git 只提交 `manifest.json`、`update_config.json`、`models/tpwVer.txt`，别把 exe 提交上去
3. **Python 脚本变更**：修改 `.py` 文件后，增量更新无法推送到已有用户，他们需要重新下载完整安装包
4. **CDN 延迟**：推送后 jsDelivr 可能有缓存（通常几分钟），急用可以手动刷新缓存
5. **安全保护**：增量更新绝不会删除 `.idea/`、`.git/`、`.venv/` 等开发目录

---

## 如果出问题了

| 问题 | 解决 |
|------|------|
| 打包报错 | 确认已激活 venv（终端显示 `(.venv)`） |
| 用户收不到更新 | 检查 `manifest.json` 是否已 push 到 GitHub |
| CDN 404 | 等几分钟或手动刷新缓存 |
| 误删开发文件 | `git checkout -- .` 恢复 |
| venv 坏了 | 参考 `incremental_update\README.md` 附录 A |
